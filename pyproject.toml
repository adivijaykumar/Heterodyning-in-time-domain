[project]
name = "heterodyning-in-time-domain"
version = "0.1.0"
requires-python = ">=3.10"

[dependency-groups]
test = [
    "numpy>=1.26",
    "scipy>=1.12",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
